<!DOCTYPE html>
<html lang="en">

<head>
    <title>All Projects - Dwarka Expressway</title>
    <meta name="description" content="Explore our premium real estate projects along Dwarka Expressway">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="flex flex-col min-h-screen bg-gray-50">

    <!-- HEADER -->
    <header class="bg-blue-600 text-white shadow-lg">
        <div class="max-w-6xl mx-auto px-4 py-4">
            <div class="flex justify-between items-center">
                <div>
                    <h1 class="text-3xl font-bold">Dwarka Expressway</h1>
                    <p class="text-blue-100 text-sm">Premium Real Estate Projects</p>
                </div>
                <nav class="space-x-6">
                    <a href="/" class="hover:text-blue-200 transition">Home</a>
                    <a href="/api/projects/view" class="hover:text-blue-200 transition font-semibold">Projects</a>
                    <a href="#" class="hover:text-blue-200 transition">Contact</a>
                </nav>
            </div>
        </div>
    </header>

    <!-- MAIN CONTENT -->
    <main class="flex-grow max-w-6xl mx-auto w-full px-4 py-8">

        <h1 class="text-4xl font-bold text-gray-800 mb-8">Our Projects</h1>

        <% if (projects && projects.length > 0) { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% projects.forEach(project => { %>
                    <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition transform hover:-translate-y-1">
                        <!-- Project Image -->
                        <% if (project.hero && project.hero.image) { %>
                            <img src="<%= project.hero.image %>" alt="<%= project.name %> - hero image" class="w-full h-64 object-cover">
                        <% } else { %>
                            <div class="w-full h-64 bg-gray-300 flex items-center justify-center">
                                <p class="text-gray-500">No image</p>
                            </div>
                        <% } %>

                        <!-- Project Info -->
                        <div class="p-6">
                            <h2 class="text-2xl font-bold text-gray-800 mb-2"><%= project.name %></h2>
                            
                            <div class="flex items-center gap-2 text-gray-600 mb-4">
                                <span>📍</span>
                                <p><%= project.location || 'N/A' %></p>
                            </div>

                            <p class="text-blue-600 font-bold text-xl mb-4">₹ <%= project.price || 'N/A' %></p>

                            <% if (project.about && project.about.content) { %>
                                <p class="text-gray-600 text-sm mb-4 line-clamp-3"><%= project.about.content %></p>
                            <% } %>

                            <!-- Gallery Preview -->
                            <% if (project.gallery && project.gallery.length > 0) { %>
                                <div class="grid grid-cols-3 gap-2 mb-4">
                                    <% project.gallery.slice(0, 3).forEach((img, idx) => { %>
                                        <img src="<%= img %>" alt="<%= project.name %> - gallery image <%= idx + 1 %>" class="w-full h-20 object-cover rounded">
                                    <% }) %>
                                </div>
                            <% } %>

                            <a href="/api/projects/view/<%= project.slug %>" class="inline-block bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition w-full text-center">
                                View Details →
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p class="text-center text-gray-600 text-lg">No projects available at the moment.</p>
        <% } %>

    </main>

    <!-- FOOTER -->
    <footer class="bg-gray-900 text-gray-300 mt-12">
        <div class="max-w-6xl mx-auto px-4 py-12">
            <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">
                <div>
                    <h3 class="text-white font-bold text-lg mb-4">Dwarka Expressway</h3>
                    <p class="text-sm text-gray-400">Premium real estate projects along Dwarka Expressway. Building dreams, creating homes.</p>
                </div>

                <div>
                    <h3 class="text-white font-bold text-lg mb-4">Quick Links</h3>
                    <ul class="space-y-2 text-sm">
                        <li><a href="/" class="hover:text-white transition">Home</a></li>
                        <li><a href="/api/projects/view" class="hover:text-white transition">All Projects</a></li>
                        <li><a href="#" class="hover:text-white transition">About Us</a></li>
                        <li><a href="#" class="hover:text-white transition">Contact</a></li>
                    </ul>
                </div>

                <div>
                    <h3 class="text-white font-bold text-lg mb-4">Contact</h3>
                    <ul class="space-y-2 text-sm">
                        <li>Email: <a href="mailto:info@dwarka.com" class="hover:text-white transition">info@dwarka.com</a></li>
                        <li>Phone: <a href="tel:+919876543210" class="hover:text-white transition">+91 9876 543210</a></li>
                        <li>Address: Delhi NCR, India</li>
                    </ul>
                </div>

                <div>
                    <h3 class="text-white font-bold text-lg mb-4">Follow Us</h3>
                    <ul class="space-y-2 text-sm">
                        <li><a href="#" class="hover:text-white transition">Facebook</a></li>
                        <li><a href="#" class="hover:text-white transition">Instagram</a></li>
                        <li><a href="#" class="hover:text-white transition">Twitter</a></li>
                        <li><a href="#" class="hover:text-white transition">LinkedIn</a></li>
                    </ul>
                </div>
            </div>

            <div class="border-t border-gray-700 pt-8 text-center text-sm text-gray-500">
                <p>&copy; <%= new Date().getFullYear() %> Dwarka Expressway. All rights reserved.</p>
            </div>
        </div>
    </footer>

</body>

</html>
